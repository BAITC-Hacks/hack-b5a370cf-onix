# Сценарий демо на 5 минут — «Аким на 5 часов»

Подготовка (до выхода): накануне выполнен `npm run check` (тесты, линтер, сборка, типы — всё зелёное), `npm run dev` запущен, в `.env.local` есть ключ LLM, открыта вкладка http://localhost:3000, язык РУС, тёмная тема выключена (лучше видно на проекторе), интернет для подложки карты проверен через http://localhost:3000/api/health. Во второй вкладке заранее открыт http://localhost:3000/results, чтобы карта уже прогрузилась.

| Время | Экран | Что делаем | Что говорим |
|---|---|---|---|
| 0:00–0:35 | Главная → План | Показываем 5 районов и стартовый Score 52.56. Открываем задачу Нуры | «Нура: школы и детсады 38 из 100, озеленение 45. У нас один бюджет и пять решений. С чего начать?» |
| 0:35–1:25 | Проект в Нуре | Сравниваем парк и школу: парк дешевле, школа убирает критический S1. Выбираем школу; объект появляется на карте | «Здесь виден сам проект и цена выбора. Участок условный; расчётные данные и правила — из ТЗ. Мы не притворяемся, что знаем доступность каждого квартала.» |
| 1:25–2:15 | План → Результаты | Добавляем остальные четыре меры: M8/Нура, M10/Нура, M12, M5/Сарыарка. Получаем Score 56.54 за 95. Нажимаем «▶ Проиграть 2 года» | «Школа строится несколько кварталов. Движок показывает, когда появляется эффект; карта меняется вместе с показателями. Правила не позволяют одновременно поставить парк и школу в Нуре.» |
| 2:15–3:00 | Стресс-тест | Проверяем собственный план. Затем раскрываем аналитику и показываем оптимум 57.24, его провал в 2 из 6 сценариев и устойчивый вариант 55.69 | «Максимальный балл не всегда лучший управленческий выбор: важно, выдержит ли план сокращение бюджета.» |
| 3:00–4:00 | AI-советник | Включаем «Прорыв теплотрассы в Алматы» и спрашиваем, чем помочь району. Показываем шаги агента и предложение | «Модель выбирает инструменты, а числа считает движок. Ответ проверяется по результатам расчёта.» |
| 4:00–4:30 | Отчёт · Рейтинг | Показываем отчёт для PDF и отправляем план в рейтинг | «Решение можно показать команде и сравнить с другими. Сервер повторно проверяет план и считает Score.» |
| 4:30–5:00 | Язык и развитие | Переключаем ҚАЗ; завершаем перспективой реальных данных | «Следующий шаг — реальные земельные участки и данные о доступности. Сейчас геометрия проекта честно отмечена как условная.» |

## Вероятные вопросы жюри и ответы

- **Откуда цифры?** Из датасета ТЗ; формула реализована в `lib/engine.ts`, контрольные значения 52.56 / 56.54 / 57.24 покрыты тестами (`npm test`).
- **Что делает AI, если он не считает?** Объясняет посчитанные факты (сильные стороны, риски, компромиссы) и в режиме агента выбирает, какие расчёты запустить. Числа в ответе автоматически сверяются с движком.
- **Что без интернета и без ключа?** Всё считается локально; анализ собирается шаблонным аналитиком; карта показывает районы без подложки. Ключ нужен только для LLM.
- **Почему устойчивый план важнее оптимального?** Оптимум 57.24 стоит 98 и невыполним при урезанном бюджете (85 или 90). Устойчивый план стоит 83, работает во всех 6 сценариях, худший случай 55.69, без событий 56.9.
- **Как масштабировать?** Данные по id: районы, меры, события. Полный перебор годится до ~15 мер; дальше — branch-and-bound или ILP (описано в README).

## Если что-то пошло не так

- Карта не грузится → вкладка «Схема» (SVG, без сети).
- LLM не отвечает → показать шаблонный анализ (появляется автоматически с пометкой) и стресс-тест, он не зависит от LLM.
- Сбилось состояние → «Сбросить» на странице «План» или открыть ссылку `/plan?p=M7.nura,M8.nura,M10.nura,M12,M5.saryarka`.
